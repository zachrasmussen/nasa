export class Date {
    constructor(data) {
        this.id = data.id
        this.date = data.date
        this.explanation = data.explanation
        this.title = data.title
        this.img = data.url
    }
}